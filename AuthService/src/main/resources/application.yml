spring:
  application:
    name: AUTHSERVICEAPP
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/psa_authdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8081
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true